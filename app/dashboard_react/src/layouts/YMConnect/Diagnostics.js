import React from 'react';

const Diagnostics = () => (
  <div style={{color:'#fff', fontSize:'2em', textAlign:'center', marginTop:'20vh'}}>DIAGNOSTICS (Componente en construcción)</div>
);

export default Diagnostics;
