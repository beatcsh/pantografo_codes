import React from 'react';

const RobotInfo = () => (
  <div style={{color:'#fff', fontSize:'2em', textAlign:'center', marginTop:'20vh'}}>ROBOT INFO (Componente en construcción)</div>
);

export default RobotInfo;
